SITE.host = "jamesmead.org"
SITE.user = 'jamesmead'
SITE.remote_dir = '/var/apps/jamesmead.org/'
SITE.feed_url = 'http://feeds.jamesmead.org/floehopper-blog'

task :default => :build

desc 'deploy the site to the webserver'
task :deploy => ['clobber', 'build', 'deploy:rsync']

# EOF
