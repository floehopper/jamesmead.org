# SITE.host = 'jamesmead.local'
SITE.host = 'jamesmead.org'
SITE.user = 'jamesmead'
SITE.remote_dir = '/var/www/jamesmead.org/'

task :default => :build

desc 'deploy the site to the webserver'
task :deploy => [:build, 'deploy:rsync']

# EOF
